
Web applications SPA MPA

HTML-> Hyper->link markup->structure

web develoed by -> Tim Turner Tee

tag - predefined keyword enclosed in angular braces types of tag paired and unpaired 

element -> combination of opening and closing tag and content is called element .

void element -> which have don't have any content.

h1-> 32px
h2->24px

Types of element on behaviour -> Block level element -> it will cover entire Depth of the ui > h1 -> height and width is apllicable > div,heading > start from the next line
Inline Element -> cover only actual space it take it will take depth of content> span-> height and width not use > span,a,input > start from the same line


Formating tag -> to format the content of text 
<physical tag>  <logical tag>
<b>    		<strong> -> have more priority
<i>		<em>
<u>		<ins>
<p>		<pre>-> belongs to monoclinic font family also consider void 			spaces
<mark> to highlight the task
<code>
<sub>(subscript) -> to specify in base of any text
<sup>(superscript) -> in power of ant text
<abbr> -> is used to show the tool tip on renedering
<address> -> to specify the address only



			Attribute -> Extra information about the element
Core Attribute						Element Specific Attribure
id -> if you want to target specific element (#)		href-> 
class -> target group of element(.)				src->
title -> tool tip for web page					<alt>->
style -> want to provide inline styling 


anchor tag -> used to redirect to pages
href -> hyper reference text specifies as hyper text
target > _balnk



li -> list item
types 1. ordered default -> number
2. unordered 
3. description list -> description term -> description data

type-> alpha , numeric , roman    //        ul -> type->square,circle,disc,none



table border="2" depricated-> th -> enclose in thead
tr -> td -> enlclosed in tbody

table element -> table head, body , caption -> to give name to table 

rowspan -> When many column data is same so we rowspan them in single column
colspan -> when we want to give space to some one coloumn of other

cellspacing -> cell spacing is done to give each cell particular space/ space between cells

cellpadding -> in each cell give the padding / give space in between content and cell border

Semantic Tag -> Uniqliy Identify the each portion /have more meaning/ boost SEO/ have more readibility

ex-> h->header, d->deatils, a->aside, m->main,mark, n->nav, s->summary/section , t->table/time, a->article, f->figure, f->footer/format


Form -> To take the user input/electronic format
components -> legend -> for heading
fieldset -> to wrap all the components or fields

input, label


form element -> fieldset->to group all the field element, input, label, select--option,legend, textarea
type value -> text, radio, checkbox, email, password, date

As when we want to click email -> go in input field so we do like in label for in which we define id given to inputfield

if we want to do know that submit button know which form is submit so give type submit

validate -> disbaled,selected,required,placeholder,minlength,maxlength
-> dropbox -> option -> selected disbaled
-> required for mandatory


In Gender to make only one select so we make all things of all types by help of name="gender" so they are type of gender

form attribute action->where the form data go after/have path , method->get(default)(data is visible in url)(less secure)/post(data is not visible in url)(less secure)

name = "name" is written as to give the data to backend, value->it hold some value



Media Tag-> embedded a QR
embedded a one page inside another
youtube videos
audio
video

QR Code -> In img give the qr api and at last in exchnage of example write your url

iframe-> to embedd one page inside another page

audio -> controls , loop, muted, autoplay

video-> to make thubnail like use poster






											CSS (cascading style sheets)(pouring) -> invented by hakon Wieum lee in 1994
											CSS1 -> 1996
											CSS2 -> 1998
											CCS3 -> 1999

Types of Css -> 1. Inline CSS(using style attribute)
2. Internal -> style tag inside your head
3. External -> differnent file in other position(head -> link tag)

2&3 is depends on priority how much is close to html tag more close more less priority 

CSS line by line execution

CSS Selector-> class(.),id(#),tagname,grouping(,),universal selector(*)



CSS Units -> Absolute(it will not change)(px,inches,cm,mm)                  Relative->size is depend on the parent container
1px -> 1/92 inches															ex->em(parent related(div -> 5px , so child have 5*5px)),rem(html(16px) related),vh,vw use them for responsive

Text Properties -> to change appearance of text
text-colorc(Rgb(red,green,blue(0-255)),rgba(alpha(intensity)),hsl(hue saturation lightining),hexadecimal(#(0-9,A-F)(3,4,6,8 range is allowed)),hsla(alpha(1-360)))
text decoration(underline,overline,solid,line-through,)
text alignment(align)
text transform(uppercase,lowercase,none,captilize)
line height(provide space b/w lines)
word spacing(provide space b/w words)
letter spacing(provide space b/w letter)
text-intent(para spacing)

Font Properties-> 
Font Size
Font weight(bold,bolder,100-900)
font family(normal(inbuild),external(cdn(content delivery network)/download/import))



Box-> flow 
margin(to move the container)
		border(space between margin and padding)
				padding(space between border and content)
						content



box - sizing -> border-box(content not come outside)

margin -> top right bottom left

margin -> top right/ledt bottom
margin -> top/bottom left/right


padding -> to move the content innside a container

padding -> 

border-> border-radius
border-top-right radius
border-width
border-top-> 2px solid red



Display Property
Block -> make the element to behave like a block level element
Inline -> make inline element
Inline-Block -> inline-block element
Flex(one directional achivement)-> display:Flex
		justify content
		align item
		row reverse/col reverse
None

flex-> display,flex wrap->wrap,justify-content->space-between,space around,space evenly








JavaScript -> high level object oriented programming language/dynimcally typed/.js extension/interpeted language
JavaScript is used for client side rendering/ synchronous in nature
JavaScript is single threaded

NextScape computer coorpartion
NextScape navigator(browser) lanaguage ->(brandon euch 1995) mocha->livescript->javascript->js(give to elcma(european computer manufacturing association))


Js give to js engine -> Parser(dividing the code into small chunks) -> Abstract Synthetical Tree(Ast)(store in the form ast)(give this code to interpeter)->interpeter->generate the byte code->machine code
																											|
																											JIT(Just in Time)(have profiler check for hot put which is repeating again again) used for performace optimization






